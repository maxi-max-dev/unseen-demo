# UNSEEN 阿里云网站部署最短教程

## 先说结论

现在的阿里云 OSS 桶负责照片和空间数据，不能拿来当公开网站桶。网页应放进一个新建的独立 OSS 桶，再绑定已备案的自定义域名。

OSS 默认域名会强制下载 HTML。无论杭州还是香港，都必须绑定自定义域名后才能正常显示网页。

这一步部署的是演示站的公网层，包括主页、宾客直传、只读展览和带口令的 Studio 演示模式。Python、CLIP、照片审核和发布写接口仍运行在主办方 Mac 上，不属于本次 OSS 静态部署。不能把它说成完整后端已经上云。

## 你在阿里云控制台只做四件事

### 1. 准备域名

优先使用已经完成 ICP 备案的主域名，再开一个子域名，例如 `unseen.example.cn`。主域名已备案时，子域名通常不用重复备案。

新买但尚未备案的域名，不能立即接到中国内地 OSS。若要赶演示，应该使用你现有的已备案域名开子域名。

### 2. 新建独立网站桶

进入对象存储 OSS，创建 Bucket。

建议配置：

* 地域选华东 1 杭州。
* 名称可用 `unseen-advx-2026-site`，若被占用就在末尾加数字。
* 关闭阻止公共访问。
* Bucket ACL 设为公共读。

这个桶必须是刚创建的空桶，只放脚本固定清单里的网页文件，不放宾客原图、审核数据、评测真值或密钥。发布脚本首次只接受空桶，重复发布只接受带 UNSEEN 标记且没有清单外对象的站点桶。

### 3. 开静态网站并绑定域名

在新 Bucket 中进入数据管理，静态页面。

* 默认首页填 `portal.html`。
* 默认 404 页面也填 `portal.html`。

然后进入 Bucket 配置，域名管理，绑定域名。

* 填你准备好的子域名。
* 域名和 Bucket 在同一阿里云账号时，选择自动添加 CNAME。
* 若不能自动添加，就按控制台给出的记录值，在云解析 DNS 中添加 CNAME。

### 4. 开 HTTPS

在数字证书管理服务中为这个子域名申请或上传证书。证书签发后，回到 OSS 的域名管理，在该域名右侧选择证书托管并上传证书。

## 做完后发给 Codex

只需要告诉我两项：

1. 完整访问域名，例如 `https://unseen.example.cn`。
2. 新网站 Bucket 的名字。

不要把 AccessKey 或配置文件内容发到聊天里。我会先在本机做不泄密的权限检查。现有凭据能否操作新桶目前未测，若权限不足，只在阿里云控制台补最小 OSS 权限，不在聊天里传密钥。

发布成功后，脚本会在本机记录这个公开域名。重启本地后端后，Studio 和邀请码页会从后端拿到同一个阿里云地址，不会继续发旧的 Vercel 二维码。
