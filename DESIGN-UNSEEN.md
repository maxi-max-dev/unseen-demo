# UNSEEN · 视觉基准规格

> 来源：Max 的美术队友出的 Demo UI 图（2026-07-25 由 Max 在对话里直接投递，共 6 张）。
> 这份文件是**把图翻译成可执行的设计参数**，供本仓库六幕演示页（`web/demo.html`）与 Codex 美术协作使用。
> 原图不在仓库里（Max 是粘贴进对话的），所以这份规格就是唯一的落盘基准。

---

## 0. 基准图里有什么（逐张）

| # | 画面 | 抽出来的东西 |
|---|---|---|
| 1 | 婚礼氛围图：新娘背影、宾客侧影、暖金逆光、漫天光斑、胶片齿孔带穿插 | 情绪基调、光斑与胶片母题 |
| 2 | 桌面 Web：`UNSEEN` 顶栏 +「A moment you never saw」故事播放器 + 时间轴多机位缩略图 + 右侧 Access & roles 面板 | 版式骨架、卡片圆角、角色药丸、渐变主按钮 |
| 3 | `UNSEEN Studio`：多设备校正时间轴、三条设备轨、波形、"Unseen Moment / 3 perspectives / 94% confidence" 面板 | 多机位对齐的可视语言、置信度徽章、暖色波形 |
| 4 | 手机三联：`64% shared` 进度环 + Memory Missions 任务卡 + "A missing piece was found" 拼图 | 进度环、任务卡、拼图母题、移动端版式 |
| 5 | 手机三联：UNSEEN 首页 + 创建记忆空间 + 邀请二维码 | 移动端首页节奏、二维码邀请、角色分级 |
| 6 | Logo：三个半透明圆（粉 / 品红 / 金）交叠，**正中交集是一颗心**，粉金渐变底 | 品牌标记，可直接 SVG 复刻 |

**最重要的一条**：Logo 的三圆交集成心，跟这个产品的论点是同一件事 ——
**三个人的视角叠在一起，中间那块才是完整的记忆。** 六幕里凡是要"点题"的地方都用它。

---

## 1. 色板（token）

```css
--u-bg-1:#FFF3F1;   /* 粉白，页面左上 */
--u-bg-2:#FFF7EE;   /* 奶油，中段 */
--u-bg-3:#FFEFE3;   /* 暖杏，右下 */

--u-pink:#FF7FA8;   /* 品牌粉，渐变起点 */
--u-pink-hot:#FF5C8A;/* 高亮粉：时间轴指针、当前项 */
--u-gold:#FFC981;   /* 品牌金，渐变终点 */
--u-gold-deep:#F6A94A;

--u-ink:#3E2430;    /* 深梅，标题 */
--u-ink-2:#5C3B48;  /* 正文 */
--u-ink-3:#9A7C86;  /* 次要说明 */

--u-card:#FFFFFF;
--u-line:rgba(255,170,185,.28);
--u-shadow:0 10px 34px rgba(214,124,140,.13);

--u-grad:linear-gradient(100deg,var(--u-pink),var(--u-gold));
```

角色药丸（图 2 右栏）：淡色底 + 饱和字，圆角全圆。

| 角色 | 底 | 字 |
|---|---|---|
| Owner | `#FEF0DC` | `#F6A94A` |
| Admin | `#E9ECFB` | `#6C7BD0` |
| Editor | `#F1E8FB` | `#9B6FD1` |
| Contributor | `#E5F5EA` | `#5BA97A` |
| Viewer | `#F0EBED` | `#8B7F85` |

本项目复用为：`自动入选` = Contributor 绿，`待你看一眼` = Owner 金，`外来照片` = Viewer 灰。

---

## 2. 字

- **字标 UNSEEN**：全大写，字重 500，字距 `.3em`，颜色 `--u-ink`。永远不加渐变，渐变留给按钮和数字。
- **大标题**：字重 700，行高 1.06，字号 `clamp(30px,7.2vw,56px)`。基准图里标题是两行短句压在一起的（"A moment you / never saw"），中文照此断行。
- **正文**：15px / 1.75，`--u-ink-2`。
- **说明**：12.5px，`--u-ink-3`。
- **数字**：大号轻字重（图 4 的 `64%`），`font-variant-numeric:tabular-nums`。
- 中文用 `PingFang SC`，西文用系统 UI sans。**不要衬线体** —— 现版三扇门是宋体气质，UNSEEN 这版是圆润无衬线，这是这次刷皮最大的口径变化。

---

## 3. 形

- 卡片圆角 **18px**，大面板 **24px**，手机外框 **40px**，药丸全圆。
- 卡片：白底 + `--u-line` 细边 + `--u-shadow`。**不用重描边**。
- 主按钮：`--u-grad` 满填，白字，圆角 14px，内边距 `14px 22px`，阴影 `0 8px 22px rgba(255,124,160,.35)`。
- 次按钮：白底 + 细边 + `--u-ink-2` 字。
- 进度环：两段弧，`stroke-linecap:round`，渐变描边，环内放大数字 + 小字说明。
- 分隔：不用实线，用 `--u-line` 或直接留白。

---

## 4. 光

基准图的辨识度有一半在光上，别省：

1. **光斑（bokeh）**：8-14 个不同大小的柔和圆，`filter:blur(…)`，粉/金/白，透明度 .12-.35，缓慢漂移。
2. **暖光晕**：页面顶部一片 `radial-gradient` 暖白，模拟逆光。
3. **胶片齿孔带**：细长条 + 等距小方孔，低透明度斜穿背景。第①幕开场和第⑥幕成片用它点题。
4. 所有玻璃感面板：`backdrop-filter: blur(14px)` + `background:rgba(255,255,255,.72)`。

⚠️ 手机上光斑动画要能一键停（`prefers-reduced-motion` 与低端机降级），不许因为装饰卡住动线。

---

## 5. Logo（可直接实现）

三个圆，半径相等，圆心成正三角（顶点朝上），互相重叠约 45%：

- 上圆：`#FF9FC0`，透明度 .55
- 左下圆：`#FF6E9E`，透明度 .55
- 右下圆：`#FFC26B`，透明度 .55
- 混合模式 `multiply` 或手工叠色；**三圆公共交集处填白并发光**，形状收成一颗心（心尖朝下）。
- 用途：字标左侧的小标记（20px）、第①幕开场中央（120px）、第⑥幕愿景收尾（96px）。

本项目的说法（可写进字幕）：**三个人的视角叠在一起，中间那块才是完整的记忆。**

---

## 6. 命名口径 ⚠️ 需要 Max 拍板

基准图上打的字标是 **UNSEEN**，仓库和 Vercel 域名也已经是 `unseen-demo`。
交接书 §7 还写着「名字待定，现在全是空间记忆(暂定)」。

本次刷皮**按 UNSEEN 走**（字标 UNSEEN，中文副题「空间记忆」），因为美术基准已经这么定了。
Max 若要换名，只改字标那一处，不影响动线和皮。

---

## 7. 不做的事

- 不为了像基准图牺牲可用性（图里是英文短句，中文更长，宽度不够就换行，别缩字号到读不清）。
- 不 1:1 复刻图 2 / 图 3 的桌面工作台（那是产品愿景图，不是今晚的动线）。
- 不引入外部字体 / CDN（离线 DemoMode 必须可走）。
