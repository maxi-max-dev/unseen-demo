# 六幕演示页 · 缺陷账本

> 规矩（建造书 §6）：修一条勾一条，**不许删记录**。每条带时间戳。
> 轮次说明：第 0 轮是建造者自查，第 1 轮起是**全新验收员子代理**（不给任何建造上下文，只给验收清单和页面地址）。

---

## 第 0 轮 · 建造者自查（2026-07-25 02:35 - 02:50）

方法：无头 Chrome + CDP `Emulation.setDeviceMetricsOverride({width:390, mobile:true})`，逐幕截图肉眼看。

- [x] **P1 · ③走进空间：封面图完全看不见，一整块深色空白**
  原因：`.frame-cover img.poster` 给了 `z-index:-1`，被推到父元素背景**后面**去了。
  修法：改成图片正常层级 + `::after` 伪元素压暗罩。`web/demo.html`
  修完复验：全景封面正常显示（`shots/p3.jpg`）。

- [x] **P1 · ④多视角：全景条左边一大块空白，像图没加载**
  原因：把全景当 `<img>` 铺，按 2:1 原比例只有 176px 宽，条子本身有 354px；且方位转到 0° 附近时没法绕回去接上。
  修法：改成 `background-repeat:repeat-x` 的背景，横向拉到条宽 3 倍，按 yaw 推 `background-position-x`。
  修完复验：整条铺满，0° 附近能绕回去（`shots/o4.jpg`）。

- [x] **P1 · 直接开 `#2` / `#5` 进来时动画不播，是一张死图**
  原因：`go()` 里用 `opts.silent` 把首次加载排除在自动播放之外了。队友很可能拿链接直接跳某一幕。
  修法：去掉 `silent`，进这一幕就播。
  修完复验：`location.hash='#2'` + reload，归位动画照播（`shots/p2.jpg`）。

- [x] **P2 · ②宾客上传：落位回执胶囊撑破手机槽**
  原因：`.pill` 是 `white-space:nowrap`，172px 的胶囊塞进 120px 的槽。
  修法：换成会换行的 `.done` 小块。
  修完复验：文字完整不裁切（`shots/q2.jpg`）。

- [x] **P2 · ④⑤幕最后一行字被右下角「演示模式」角标压住**
  原因：角标 `position:fixed` 浮在右下角，而这一页每一幕底部都有话要说。
  修法：角标搬进顶栏；窄屏（<430px）隐藏字标里的「空间记忆」腾位置。
  修完复验：顶栏放得下，正文再没被压（`shots/q2.jpg`）。

- [x] **P1 · ⑤主办后台：投稿人显示成「延迟测试」「你大爷」**
  原因：s4 空间里混着上一轮压测留下的假名字，直接显示出来像产品在胡说。
  修法：导出脚本 `tools/build_demo_data.py` 里把这类名字统一显示成「匿名宾客」。
  **只改显示的署名，方位 / 匹配度 / 辨识度 / 状态一个数字都没动。**
  修完复验：署名只剩 小明 / 小红 / 阿伟 / 大伟 / 匿名宾客。

### 第 0 轮里**不是**缺陷的（记下来免得下一轮重复误判）
- 「读数四行 opacity 计算值是 0，但 class 上有 `.in`」——这是**无头 Chrome 后台 tab 冻结渲染帧**导致 CSS 过渡卡在中间态，
  强制一帧之后 `getComputedStyle` 立刻变 1。属于测试环境伪影，不是页面 bug。
  **下一轮验收：截图前先跑一条 `eval "1"` 强制一帧。**

---

## 第 0.5 轮 · 建造者自查（03:20 - 03:50）

- [x] **P0 · ③走进空间：本地直接打开（file://）时整幕死掉，显示「全景图加载失败」**
  原因：3D 漫游要用 WebGL 贴全景纹理，`file://` 下浏览器按跨域拦掉。
  而「断网也能走完」是建造书 §8 列的**永远不砍**的一条，这一幕等于没了。
  修法：第③幕主内容换成 **2D 方位墙**（`<img>` 底图，哪都能开、手机上不吃帧）：
  全景横铺一圈可拖动、7 张已入选照片钉在各自方位、引线落到刻度条、
  开场自动定位到照片最密的一段、方位撞车的横向错开。3D 漫游降级成按钮。
  修完复验：`file://` 与 `http` × 390px 与 1280px，六幕全过，零报错。

- [x] **P2 · ③方位墙开场停在一段空墙上（只看得见 1 张照片）**
  原因：`pwBestYaw()` 里角度距离的比较写反了（`(180-d)<=60` 应该是 `d<=60`）。
  修完复验：开场可见 4 张（194.9° / 225° / 255.3° ×2）。

## 第 1 轮 · 独立验收员（03:00 - 03:55，全新子代理，不给建造上下文）

⚠️ 这一轮有个方法问题要记下来：**验收员在跑的时候我还在改第③幕**，
所以它的报告横跨了两个版本（它报的行号已经对不上）。下一轮改成「先冻结再验」。

- [x] **P1 · hash 深链接在页面已加载后失效**
  它的复现：在已经开着的 tab 里把 `location.hash` 改成 `#5`，画面不动；浏览器后退键同理。
  原因：`go()` 只往外写 hash，从没监听 `hashchange`，只在启动那一刻读一次。
  修法：加 `hashchange` 监听。
  修完复验：`#5 → act5`、`#3 → act3`、`history.back() → act5`，全对。

- [x] **P2 · 快速连点翻幕时抓到过一次 `Cannot read properties of undefined`（16 次里 1 次，之后 15 轮没能复现）**
  定位不了：它报的行号属于我当时正在改的那一版，现在那一行是别的代码。
  但它指出的竞态点是真的：`setTimeout(pwLayout, 60)` 这类回调没确认「人还在这一幕」。
  修法：所有进幕后的延时回调统一包一层 `onAct(k, fn)`，不在这一幕就不执行。
  修完复验：连点 next 140 次 + hash 乱跳 90 次，零报错。

- [x] **P2 · 归位动画「顺不顺」它验不了（无头 rAF 被节流到约 1fps）**
  它判的是「没验成」而不是 FAIL，这个判断是对的。
  我这边补测：驱动脚本每次 attach 都发 `Page.bringToFront` + `setFocusEmulationEnabled`，
  这条车道的 rAF 实测 83fps，不受节流，所以能量。用 rAF 逐帧采 `getComputedStyle` 的
  transform 矩阵，连测三轮：
  **飞行 1.77 秒，97 到 103 个不同位置，62 到 66 fps，帧间隔中位数 10 到 11ms，行程 491px。**
  也就是说它是真在逐帧插值，不是一步到位。
  顺手修掉一个真隐患：`#flyer` 的 transition 里挂了 `width`，
  **width 会触发布局、逐帧重排**，正好卡在飞行那一秒半上。缩放本来就是 `transform: scale()` 做的，
  那条纯属多余，已删。（另：帧间隔偶发 78 到 98ms 的毛刺，无头环境里测不准，
  **这一条最终还得 Max 在真机上亲眼看一遍**，自动化在「顺不顺」上不可信。）

- [x] **P2 · 入口页罗盘写「共 40 张」，队友可能当成真实婚礼现场统计**
  它翻了 `tour.js` 才发现那是 `fixtures.py` 生成的合成集。
  修法：文案改成「共 40 张**占位素材**」。

### 验收员确认没问题的（存证）
- A 六幕动线手机 + 桌面：PASS　- B DemoMode（file:// 与 onLine=false）：PASS
- C 六幕字幕逐条抄录核对：PASS　- E 第④幕三机位切换：PASS
- F 第⑤幕数字对 `space.json`：**逐条 PASS**（11 / 7 / 4 / 2·7 + 四张待审的匹配度辨识度全对上）
- H 390px 零横向溢出：PASS　- J 无点赞评论：PASS
- 它还独立核了第⑥幕「12 张有 11 张落点」那句：`server/spaces/s4/photos/` 里 p7.jpg 存在但没进
  photos 数组，12 个文件 11 个有落点，**这句话是真的**。
- 它一度怀疑第⑤幕 t4 署名「匿名宾客」和 `space.json` 的 `filledBy:["延迟测试"]` 对不上，
  查了导出脚本的注释后自己排除了。说明那条注释写对了地方。

## Codex 对审（建造书 §6-5，04:20）

前两次派活都**静默死掉**（交接书里写过这个老毛病，这轮又中了两次）：
第一次跑去折腾浏览器技能，只写了个空壳；第二次让它查 P0，读到一半进程没了。
第三次把范围收到「只读一个文件、四个问题、先写结论」才拿到东西。

**它确认没问题的（存证）**
- `pol()` 的「0° 朝上、顺时针」数学正确
- `yawRange = [300, 59]` 这种跨 0/360 的区间，被正确画成顺时针跨零点的 119° 圆弧（没画反、没切成两段）
- 第③幕方位墙 `pwRender()` 的取模绕回逻辑覆盖两侧边界，没有钉子消失或错位
- 全文件没有 CDN / 远程字体 / 远程图片（`w3.org` 那个只是 SVG 命名空间）

**它报的唯一 P0：`yaw` 可能为 null 时 `.toFixed()` 会抛异常 → 查证后不成立**
- 导出脚本里 `"yaw": round(float(p.get("yaw") or 0), 1)`，**照片的 yaw 永远是数**。
- 逐个核了 demo.html 里全部 20 处 `.toFixed()`：落在数据字段上的**全部是照片 yaw**；
  `confidence` / `margin` 都有 `!= null` 保护；任务的 yaw（可能为 null）没有任何一处被 `.toFixed()`，
  墙上用的是 `t.bearing ? ... : t.title` 的降级。
- **但它指出的问题本身是对的**：这个契约当时只写在注释里。
  已改成在导出脚本里**出厂自检**，数据不对就不生成，别让页面拖到明早才炸。

## 第 1.5 轮 · 建造者自查：窄屏扫描（03:38）

390px 只是一个点，真机宽度是一条谱。扫了 320 / 360 / 390 / 412 四档：

- [ ] **P1 · 320px（iPhone SE 这一档）第②幕横向溢出 20px**（360 / 390 / 412 都干净）
  原因：读数卡片里那句「数字来自 `server/spaces/s4/space.json`」是个 26 字符的**长路径，
  中间没有可断行的地方**，把 `.readout` 的内容宽度撑到 173px 以上，
  而 320px 下 `.duo` 右列只有 153px。栅格用了 `minmax(0,54fr)` 能收，收不住的是这条不可断的字符串。
  证据：`#act2 .line` / `.tiny` 的 `right` 实测 340 > 视口 320。

## 第 2 轮 · 独立验收员（03:20 - 03:46，全新子代理，构建已冻结）

- [x] **🔴 P0 · 第②幕那张飞行的照片，全程渲染在视口外，观众根本看不见「照片飞回方位」**
  这是这一轮最重要的一条，**而且它推翻了我自己上一轮的结论**。
  它的证据：动画期间逐帧读 `#flyer.getBoundingClientRect()`，
  390×844 视口下 `top` 全程在 **1023 到 1498px** 之间，永远在屏幕下方；
  同一时刻罗盘的 `top` 才 140px。
  根因：`#flyer` 是 `position:fixed` 但**没写 top/left**。
  CSS 规范里 `top/left` 为 `auto` 时，起始盒子取的是它在文档流里的**静态位置**，不是视口原点；
  而 JS 里 `translate()` 传的是 `getBoundingClientRect()` 算出来的**视口绝对坐标**。
  两者叠加，整张卡片被推到视口下方一千多像素。
  落位后罗盘的读数是另外一套 DOM 写入，跟 `#flyer` 无关，所以**结果看着是对的，过程完全不见**。
  修法：`#flyer` 补 `top:0; left:0`。
  修完复验：107 帧里 **27/27 亮着的帧全部落在视口内**，路径
  `(53,597) 宽118 → (88,443) → (131,260) → (150,181) → (157,152) 宽57`，
  从手机升到罗盘、边飞边缩，**并且截到了半空中那一帧**
  （`验收包/逐幕截图/幕2归位动画-半空中.jpg`）。

  🧨 **我要认的错**：上一轮我说这个动画「量过了，1.77 秒、97 到 103 个不同位置、62 到 66fps」，
  那些数字本身没错，但我量的是 **transform 的插值数值**，
  **从没验过这个元素到底渲染在哪**。等于犯了交接书开头写的那条：「日志零报错 ≠ 画面正常」。
  验收员用 `getBoundingClientRect` 一测就穿。**它是对的，我是错的。**

- [x] **P2 · 第⑥幕「12 张有 11 张落点、偏差 0 到 2 度」这句找不到出处**
  它查了 `film/assets/shots.json`：`photoCount=12` 对得上，但 12 条 reveal 记录的
  `confidence` 全是 1.0，没有任何偏差字段，「11/12」和「0 到 2 度」在仓库里找不到来源。
  查证：这组数来自**交接书 §4-②**，是上一轮一个独立验收员自己写暴力搜索量出来的，
  原始日志确实没留在仓库里。
  修法：**不删这条，但把来源摆到明面上** —— 改写成「另据上一轮独立验收员的记录……
  那次的原始日志没留在仓库里，这条只算旁证，不当本页的实测数字用」。
  本页其它数字都能追回 `space.json`，这一条不能，就不该和它们长一个样。

### 第 2 轮确认没问题的（存证）
A 手机+桌面六幕 / B `file://` 六幕（含第③幕的降级提示与 `window.open` 兜底）/
C 六幕字幕逐条抄录 / E 三机位联动（角度 15.1→194.9→255.3、匹配度 0.88→0.92→0.89 都跟着变）/
F 第⑤幕四个统计数逐条对上 `space.json` / H 六幕零报错且 `scrollWidth` 恒等于 390 /
I 深链接与前进后退 / K 连点 100 次 + 乱跳 hash 60 次零报错 / J 无点赞评论。
它还用 `ffprobe` 实测了成片时长 **79.445 秒**，确认「79 秒」不是编的。
它这一轮的 rAF 实测 **73.5fps**，所以 G 项它敢判 FAIL 而不是「没验成」，判得对。

## 第 2.5 轮 · 建造者顺藤摸瓜（04:05）

抓到 `#flyer` 那个「fixed 但没写 top/left」的坑之后，把**同一类**的另外两个元素也验了一遍
（都是 `position:fixed`、都参与归位动画）：

- ✅ `.burst`（落位的光爆）：4 帧全部落在视口内，位置 (171,163)，正对罗盘落点。**没问题。**
- ⚠️ `.beam`（从罗盘圆心射向落点的光线）：**没坏，但很弱。**
  实测它确实亮起来（2243ms 到 2541ms，opacity 峰值 0.958，几何也对：
  left/top 正好是罗盘圆心 (195,243)，长度 79px 指向落点）。
  问题是**淡入用了 .4 秒，而它只被显示 0.66 秒就被摘掉**，等于刚亮到头就没了，是一下闪。
  （我第一次量它时报「0 帧亮着」是我自己的探针写错了：阈值卡 0.05，
  而它只在最后 200ms 才越过 0.05。**不是页面的问题，是我的测法问题**，记在这儿免得下次误判。）

- [ ] **P2 · 光线太短促，建议淡入改 .2 秒、显示窗口拉长到 1.1 秒左右**
  **今晚故意不改。** 理由：第 3、4 轮验收员正在测这个构建，
  为了一条 300 毫秒的装饰去破坏「冻结构建 + 连续两轮全绿」的流程不划算。
  留给 Max 醒后决定，改动量是两个数字。

## 第 4 轮 · 对抗式验收员（04:03，专门交代它「去弄坏它」）

它没有信账本上「都修好了」的说法，每一条都自己重打了一遍，另外按五类攻击面单开。
**结果：又打坏两处。**

- [ ] **P1 · 第③幕方位墙：同方位的两张照片，后一张的图片盖住前一张的名字标签**
  它的证据：直接打开 `#3` **不用任何操作就 100% 复现**（方位墙默认定位到照片最密的那一段，
  而那一段恰好有两张都在 255.3°）。320 / 390 / 768 / 1280 全复现，跟宽度无关。
  我复核了几何，确认到像素：
  第二个 255.3° 钉的**图片** `l216 r294 t286 b342`，压住第一个钉的**标签**
  `l176 r243 t326 b345`，**重叠 27×16px**，「大伟 · 255°」右半截被盖。
  它把根因归到「横向只错开 46px，不够躲开更长的名字」——**方向对，但结论要修正**：
  真正压住文字的不是相邻标签，是**后一个钉的图片**（图片 56px 高，
  纵向错开只有 22px，压得住前一个钉的标签）。
  ⚠️ 它建议的「把横向偏移加大」我**不打算采纳**：46px 在这条墙上约等于 15.6°，
  加到能躲开就得 78px 约等于 26°，**等于把照片钉到一个它并不在的方位上**，
  这跟这一幕「照片钉在它被拍下的方位」的主张直接冲突。
  改法改成：**同方位（7° 内）的照片并成一个钉**，卡片叠着画，标签写「大伟 等 2 人 · 255°」。
  既消掉碰撞，又比原来更诚实，而且「两个人拍了同一个方向」本来就是这产品要讲的事。

- [ ] **P1 · 第②幕：20 次亚 420ms 的快速进出后，读数面板可能卡在「揭晓了一半」的自相矛盾态**
  它的证据：罗盘中央还是占位的「—」和「等一张照片」，右边读数卡却已经亮出匹配度、辨识度、
  判定三行，唯独「方位」那行没出现。持续 1.5 到 2.2 秒后自愈。
  根因：`onAct(k, fn)` 只判「当前幕是不是 ②」，**不判「这个计时器是不是这一次进入产生的」**；
  而 `playFly` 内部那串 setTimeout 根本不认幕、也不会被取消。
  反复横跳时旧链和新链交叉执行，谁先重置、谁后揭晓全看运气。
  改法：给 `playFly` 加**世代号**，新一轮开始就让旧一轮的残留计时器全部失效，
  不再只靠 `flying` 这个布尔位。

### 第 4 轮确认干净的（存证，含它主动排除的假阳性）
- `#flyer` 的 P0 **没有回归**：390 手机 + 1280 桌面逐帧 `getBoundingClientRect` 全在视口内，
  并且它自己也实拍到了一张半空中的图。
- 罗盘 7 根刻度全部落在圆环范围内。
- **六档宽度 320/360/390/412/768/1440 × 六幕**：零横向溢出、零文字裁切。
  390×568 超矮视口下有按钮初始在字幕条下方，但**页面可滚、滚到底全部可达**，不算永久遮挡。
- 连点「再放一遍」打断飞行 ×10、方位墙拖到一半切幕、连点下一幕 ×130、乱跳 hash ×70：
  全部零报错、终态正确。
- 数字：空间统计、四张待审的匹配度辨识度、0.9158 / 0.1019、成片时长（`ffprobe` 实测 79.445s）、
  阈值标定的 24/23/15/15（能追到 `server/space.py` 注释）**逐条对得上**。
- `file://` 六幕零报错，第③幕给的是人话解释不是白屏。
- 🟢 **它主动排除了一条自己造出来的假阳性**：手写 `PointerEvent` 模拟拖动时报
  `setPointerCapture: No active pointer with the given id`，换成 CDP `Input.dispatchMouseEvent`
  （更贴近真实输入管线）重跑就没了，判定是合成事件的伪影，不计入缺陷。**这个自觉很值钱。**

## 第 3 轮 · 独立验收员（完整清单，04:23）

**没有 P0，没有 P1。** 只有两条 P2，其中一条还是产品建议不是缺陷。

它独立复核了第②幕那个 P0 已修复，而且做法比我更硬：
**不用 JS 合成的 `.click()`，改用 CDP `Input.dispatchMouseEvent` 真实派发鼠标事件**
（更贴近真手指），24 点全程采样 `top` 从 568 一路平滑降到 117、`left` 从 49 升到 158，
全程落在视口内；rAF 实测 **79.4fps**、帧间隔中位数 11.3ms；
并且自己截到了半空中那一帧（`verify3/mobile_act2_midflight_REAL.jpg`）。

它还报了一个**方法论发现**（不是产品缺陷，但很值钱）：
无头 Chrome 空闲 tab 里**单次 `requestAnimationFrame` 的延迟能长到 2141ms**（它实测的），
而这段动画恰好只用一次 rAF 去触发 CSS transition，所以用 JS 合成 `.click()` 测时
会偶发「动画卡在起点、但文字读数照常刷到落地值」——
换成真实鼠标事件后多轮不再复现。**这解释了第 1 轮那次「一次性诡异现象」的真正来源。**

- [x] **P2 · 第③幕首屏进来时，最左边那张照片的名字被切掉半个**
  它明确排除了「布局溢出」（`scrollWidth` 没溢出，容器 left=18 right=372 完全在 390 内），
  判定是横向可拖拽全景条的**初始位置**问题。
  查证后根因**不在取景算法**，而在 `pwNudge()` —— 那个「提示这里可以拖」的动效
  **单向漂了 14% 一圈（约 148px）且不回来**，把前面精心挑好的首屏取景整个顶歪了。
  修法两条一起上：①`pwNudge` 改成荡出去**再荡回原位**（`sin(kπ)`，k=0 和 k=1 都回到起点）
  ②首屏取景改成在最密处附近扫一圈候选位移，挑「完整露出来的钉最多」的那个（切一半罚分比少看一个重）。
  修完复验：390px 下可见 4 个钉、最左边「匿名宾客 · 165°」完整可读；
  1280px 下 0 个被切；右缘那个半露的是横向滚动条该有的「还有更多」暗示，不是缺陷。

- [ ] **P2（产品建议，不是缺陷）· 第⑤幕叙事节奏最弱，像后台运维面板插进了故事中间**
  它的话：前四幕是一条完整情感线（错过的五分钟 → 扫码归位的魔法 → 挂满照片的空间 → 换一双眼睛），
  到第⑤幕突然切成数据仪表盘，是六幕里唯一一幕「讲道理」而不是「讲故事」的。
  它同时强调**数字别动（都核过是真的）**，要换的是包装。
  **今晚不动。** 这是叙事口径，属于 Max 拍板范围（建造书 §5 也写了「要换词只动字幕层」）。
  已写进验收包让他决定。

### 第 3 轮确认干净的
A 手机+桌面六幕 / B `file://` 六幕（第③幕的跨域提示与 `window.open` 兜底都对）/
C 六幕字幕逐条抄录 / E 三机位（0.88→0.92→0.89 实测变化）/
F 第⑤幕数字逐条精确匹配 / H 四宽度零溢出零报错 /
I hash 跳幕 + `back()` + `forward()` / K 连点 120 次 + 乱跳 70 次零报错 / J 无点赞评论。
第⑥幕那句旁证的措辞它专门点评了：「页面自己标注了原始日志没留在仓库里、只算旁证，
这个措辞本身就很诚实，没有拿不能验证的数字冒充实测」——说明那处改对了。

---

## 第 5 / 6 轮 · 两个独立验收员并行复验（构建已冻结在 47ba157）

第 3 轮全绿、第 4 轮打坏两处，所以还不算「连续两轮全绿」。
两条 P1 + 第 3 轮那条 P2 全部修完后重开两轮：第 5 轮走完整清单，
第 6 轮对抗式、并**点名复打刚修的三处**（飞行坐标 / 钉子遮挡 / 时序竞态）看有没有回归。

### 第 5 轮（完整清单，04:56）：**零缺陷，A/B/C/E/F/G/H/I/K/J/L 十一项全 PASS**

关键复验数据（全是它自己量的）：
- **G 归位动画**：rAF 实测 **69.3fps**；逐帧 `getBoundingClientRect` 得 `top∈[148,615]`、
  `left∈[49,158]`，**全程在 390×844 视口内，无负值无越界**；自己截到半空中那张；重放正常。
  → 第 2 轮那个 P0 **确认没有回归**。
- **L 钉子遮挡**：图片矩形 vs 别人的标签矩形两两交叠检测，手机端和桌面端都是 `overlaps: []`；
  首屏最左边那个钉「匿名宾客 · 165°」`scrollWidth === clientWidth`，**没被省略号截断**。
  → 第 4 轮那个 P1 **确认没有回归**。
- **H**：320/360/390/412 六幕全部 `scrollWidth - clientWidth = 0`，
  连第②幕动画期间连续轮询 3.6 秒也一直是 0。
- **F**：自己手数 `space.json` 得 11/7/4 和 7/2/5，和页面 KPI 逐一对上。
- **第⑥幕 79.4 秒**：它用 `ffprobe` 实测 `79.445` 秒，分毫不差。

📌 它抄字幕时把第⑥幕那句写成了「11 张里 11 张」，**我回查了页面和源码，实际是「12 张里 11 张」**，
是它转述时的笔误，不是页面出错（`web/demo.html:1196` 与页面 `filmNote` 文本均已核对）。

📌 它报了一条**测试环境的坑**（不是页面缺陷，但下次用这套工具要注意）：
同一个 Chrome 实例里攒了 16 个并行 tab 之后，会触发无头 Chrome 的后台 tab 丢弃/重建，
**重建的渲染进程不继承 `Emulation.setDeviceMetricsOverride`**，
于是视口会从 390×844 悄悄变成 1280×813 —— 看着像页面 bug，其实是驱动脚本攒 tab 攒出来的。
`cdp.mjs` 的 override 只在 `open` 时下发一次，所以**每轮测完要 `close` 掉 tab，别攒着**。

### 第 6 轮（对抗式，05:00）：三处重点**全部确认无回归**，另在窄屏抓到两条 P2

**无回归确认（它自己量的）**
- 第②幕飞行坐标：390 端从起飞 (49,615) 飞到落点 (155,160)，1280 端 (375,738) → (597,173)，
  18 帧全在视口内，两张半空中的截图都亲眼看到卡片悬在罗盘正上方。
- 第③幕**同一钉内**的图片压名字：已消除。
- 第②幕时序竞态：3 组 × 20 次快进快出 + 飞行中连点 10 次重放，**一次矛盾态都打不出来**。
- 另外：矮视口 390×568 六幕滚到底，内容离字幕条还有 40 到 116px 余量，没有够不到的东西；
  连点 130 次、乱跳 hash 80 次零报错；178 个网络请求 0 个失败。

**新抓到的两条（都只在 320 / 360 这两档窄屏出现，390 以上干净）**

- [x] **P2 · 顶栏六个跳幕圆点被挤出屏幕，点不到**
  320px：圆点 5 在 320-342、圆点 6 在 348-370，**整个在视口外**；360px：圆点 6 被切掉近一半。
  修法：≤380px 隐藏「演示模式」徽章（装饰性标签让位给能点得到的圆点）+ 圆点缩到 19px、间距 5px。
  ⚠️ **我第一版把这段媒体查询写在 `.dots` 基础规则前面，白写了** ——
  同特异度按源码顺序决胜，19px 被基础规则的 22px 盖掉，当时只是靠「隐藏徽章」侥幸放得下。
  已挪到基础规则之后并在注释里写明原因。
  复验：320/360 圆点宽实测 19px，六个全部在视口内。

- [x] **P2 · 方位墙相邻钉（相差约 30°）在窄屏互相压标签**
  320px 量出 3 组交叠，其中 2 组把邻居标签的「°」实打实吃掉；360px 2 组约 6px 只是碰边。
  根因：全景条最小宽度 900px 时，30° 只隔 **75px**，比钉子本身的 **78px** 还窄，必然压。
  （上一轮只解决了「同一个钉内」的遮挡，没覆盖「不同钉之间」。）
  修法：最小宽度 900 → **1140**（30° 拉到 95px）+ ≤400px 把钉缩到 64px、标签限宽 92px；
  引线高度里写死的图片高度 56 也改成跟着窄屏取 48。
  复验：320/360/390/412 四档，跨钉两两交叠 **0 条**。

### 🧨 第 6 轮顺带逼出的一个**工具自身**的严重问题
复验这两条时，改完代码测出来「没生效」，一查发现：
**`cdp.mjs` 没关缓存**。python `http.server` 只给 `Last-Modified`，
Chrome 按启发式新鲜度**直接吃缓存不回源**——边改边测会读到旧文件，
更坏的情况是**把旧版本当成新版本验过了**。
已在 `cdp.mjs` 的 `open` 里加 `Network.setCacheDisabled`。
✅ 好在几轮独立验收员各自用的是**全新的 user-data-dir**（chrome-v2 到 v7），首次加载没有缓存，
所以**它们的结论不受这个坑影响**；受影响的只可能是我自己那条从 02:38 一直复用的车道。
关键结论（P0 修复、并钉、竞态）都有独立验收员在干净环境里复核过，故不需要推翻重来。

---

## 📌 两条产品建议（都不是缺陷，都留给 Max 拍板，今晚一律不动）

1. **第③幕的 3D 漫游点击链路太长**（第 5 轮提的）：
   http 下先点「进去 3D 漫游」，进 iframe 后还要再点一次「点击锁定视角」，
   容易让人以为 2D 方位墙就是全部，错过真正兑现「空间」二字的那一下。
   **不改的理由**：默认内嵌 3D 会让每个进第③幕的人都吃一次 WebGL 加载，手机上尤其亏，
   而这正是当初把它降级成按钮的原因（那条 file:// 的 P0 也是这么逼出来的）。

2. **第⑤幕的叙事节奏最弱**（第 3 轮提的）：
   前四幕是一条完整情感线，到第⑤幕突然切成数据仪表盘，是六幕里唯一一幕「讲道理」不是「讲故事」的。
   提这条的验收员同时强调**数字别动（都核过是真的）**，要换的是包装。
   **不改的理由**：这是叙事口径，建造书 §5 写明「要换词只动字幕层」，属 Max 的活。

## 第 7 轮 · 最终确认（构建冻结在 30c4651，05:24）：**「这一版能交。」零新缺陷**

- **A 顶栏圆点**：320/360/375/390/412 五档六个圆点全在视口内；
  真鼠标点圆点 5、6 都真的跳到 `#5`/`#6`（`act5on:true` / `act6on:true`）。
  它还特意测了断点两侧 **379 / 380 / 381px**：380 及以下是 19px + 隐藏徽章，
  381 起变回 22px + 显示徽章，**切换那一瞬间六个点依然全在视口内**，
  没有出现「切回大号突然又溢出」的接缝问题。
- **B 方位墙钉子**：六档宽度先测初始视口，又用真鼠标**把方位墙拖满一整圈**（6 个钉组全覆盖），
  逐帧测「所有图片矩形 vs 所有标签矩形」，全程 `overlapCount: 0`。
  肉眼确认 320/360 截图里「°」和人名都完整。
- **C 回归**：归位动画 320/390 全程在视口内 + 半空中截图；时序竞态 2 组 × 20 次零矛盾；
  六幕 × 四宽度 24/24 零溢出零报错；`file://` 六幕正常；
  第⑤幕数字 total 11 / auto 7 / review 4 / tasksOpen 2 / tasksTotal 7 / tasksFilled 5 全部对上；
  连点 130 次 + 乱跳 hash 75 次零报错。

**它额外自己加的几拳（都没打坏）**
- 在 `pwNudge` 动画进行到一半时触发 `resize` 把宽度从 390 砍到 320 → 重排后零重叠零溢出零报错
- 80ms 间隔连点「再放一遍归位」15 次（远小于一个完整动画周期）→ 无鬼影、无状态撕裂
- 零间隔连点六个圆点 1→2→3→4→5→6 中途不等待 → 稳定停在 `#6`，没卡在中间态
- 顺手探了 **280px**（比规格最窄档还窄）→ 六个圆点依然全在视口内、零溢出

它的「只能再改一处」：**没有必须改的**；硬要挑就是第⑥幕那段旁证文字略长，属内容打磨。

## 第 8 轮 · 同版本第二次完整验收（05:44）：**抓到一条前七轮全漏的 P1**

§6-4 要求连续两轮全绿。第 7 轮说「能交」，我没就此收工，
又开了第 8 轮走完整清单，并**明确告诉它「前一个人说能交，不要因此放水」**。
结果它抓到了一条**前面七轮全部漏掉**的真问题。

- [x] **🟠 P1 · 按浏览器返回键会直接退出整个演示页，而不是退到上一幕**
  它的复现：干净打开 `demo.html`（不带 hash），点三次「下一幕」到 `#3`，然后 `history.back()`
  → `location.href` 变成 `about:blank`，**整个页面被卸载**，人被踢出演示。
  它量的：`history.length` 全程只有 **2**，点几次下一幕都不涨。
  根因：`go()` 翻幕一直用 `history.replaceState`，**六幕从来没进过浏览器历史栈**。

  🔍 **前七轮为什么全漏了**（这条值得记住）：
  它们测 back/forward 的方式都是先 `location.hash='#5'` 再 `'#3'` 然后 `back()` ——
  **改 hash 这个动作本身就会往历史里塞条目**，所以 back() 看着完全正常。
  只有**从「点按钮翻幕」开始测**才会暴露。
  第 8 轮是唯一一个把「用真鼠标点按钮」和「按返回键」这两件事**接在一起**测的。

  影响面：正常点按钮翻幕不受影响；但只要有人按返回键，
  **或者手机上那个几乎是肌肉记忆的边缘滑动返回**，就会被踢出演示。
  而「发个链接让队友自己看」正是这一页最主要的分发方式，所以这条必须修。

  修法：用户主动翻幕改用 `pushState`；浏览器自己造成的跳幕（`popstate` / `hashchange`）
  走 `silent` 不再 push，免得和浏览器打架；开场那一幕也 silent（它是历史栈第一条）。
  另加 `popstate` 监听。
  复验：点三次下一幕 → act4，返回 → act3，返回 → act2，前进 → act3，
  `history.length` 2→5 正常增长，页面全程没被卸载；
  深链接直开 `#4`、粘 hash `#6`、连点 125 次、`file://`、六幕 × 320/390 回归全部零报错零溢出。

**第 8 轮确认干净的**：A/B/C/D/E/F/G/H/I/K/L/M 除 J 外全 PASS。
其中 D 项 rAF 实测 **71fps**、124 个可见帧全部在视口内；
G 项六档宽度跨钉交叠 0；F 项数字全部对上；M 项入口页真鼠标点击能进 demo。

📌 它提的一条值得记的观察（不算缺陷）：`demo-data.js` 把两个用测试脏话昵称的贡献者
统一显示成「匿名宾客」，和另一个**真正匿名**的贡献者用了同一个显示名。
它的说法很准：「这是数据脱敏不是数字造假，但如果较真『页面上每个署名都对应一个真实原始署名』
这条隐含承诺，它是不成立的」。已在验收包里向 Max 交代过这件事。

## 第 9 / 10 轮 · 新构建（097bffe）双轨终验

返回键那条修完后构建变成 `097bffe`，「连续两轮全绿」的计数再次重新起算。
并行开两轮：第 9 轮走完整清单，第 10 轮对抗式、
**头号重点都是刚改的浏览器历史**（这类改动最容易引入新坑），
并特别交代第 9 轮：**必须从「点按钮翻幕」开始测 back，不许用改 hash 的方式代替**
（那正是前七轮漏掉这条的原因）。

（等结果后填。）
